import { ReservadoDirective } from './reservado.directive';

describe('ReservadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ReservadoDirective();
    expect(directive).toBeTruthy();
  });
});
